worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /run/nginx.pid;


events {
    worker_connections  2048;
}

http {
    upstream srh_backend {
        server 04-redis-tweproxy-srh-srh-1:80;
        server 04-redis-tweproxy-srh-srh-2:80;
        server 04-redis-tweproxy-srh-srh-3:80;
        server 04-redis-tweproxy-srh-srh-4:80;
        server 04-redis-tweproxy-srh-srh-5:80;
        server 04-redis-tweproxy-srh-srh-6:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://srh_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
